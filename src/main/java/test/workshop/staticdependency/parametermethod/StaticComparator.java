package test.workshop.staticdependency.parametermethod;

import test.workshop.Data;

public class StaticComparator {
    public static boolean isFirstSmaller(Data first, Data second) {
        /* complex logic */
        return false;
    }
}
